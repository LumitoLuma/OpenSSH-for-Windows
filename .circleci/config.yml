 version: 2.1

 orbs:
  win: circleci/windows@2.2.0

 jobs:
   build:
     executor: win/default     
    
     steps:
       - checkout
       - run: g++ ISCompiler\ISCompil.cpp -o ISCopmiler\ISCompil.exe
       - run: cd ISCompiler && ISCompil.exe
