version: 2.1

orbs:
 win: circleci/windows@2.2.0

jobs:
  build:
    executor: win/default

    steps:
      - checkout
      - run:
          name: Install
          command: cmd /c install.bat
      - run:
          name: Install changing execution policy
          command: cmd /c install-cxp.bat