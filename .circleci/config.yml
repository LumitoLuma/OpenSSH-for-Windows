version: 2.1

orbs:
 win: circleci/windows@2.2.0

jobs:
  build:
    executor: win/default

    steps:
      - checkout
      - run:
          name: Install
          command: install.bat
      - run:
          name: Install changing execution policy
          command: install-cxp.bat